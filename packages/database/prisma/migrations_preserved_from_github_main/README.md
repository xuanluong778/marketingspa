# Preserved Prisma migrations from GitHub `main`

These SQL files existed on `origin/main` and were replaced in production by the
squashed baseline `migrations/00000000000000_init`.

They are kept here for audit/history only. Do **not** run them with
`prisma migrate` on the live database (already baseline + db push).
