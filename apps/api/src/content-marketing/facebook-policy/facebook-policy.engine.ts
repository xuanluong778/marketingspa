/** Compile stub matching apps/api/dist — full source was empty in git/rescue/release. */
export function runFacebookPolicyEngine(..._args: unknown[]): { findings: never[] } {
  return { findings: [] };
}
