'use client';

/** Lightweight toast helper — Affiliate flows use inline messages; keep export for imports. */
export function showAffiliateToast(_message: string) {
  /* no-op: dashboard uses local msg/err state */
}

export default showAffiliateToast;
