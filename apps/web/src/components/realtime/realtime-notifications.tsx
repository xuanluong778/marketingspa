'use client';

/**
 * Đã tắt toast tin nhắn góc dưới.
 * Badge + dropdown nằm ở MessagesHeaderIcon (Header).
 * Giữ export để tránh import cũ bị gãy.
 */
export function RealtimeNotifications() {
  return null;
}
